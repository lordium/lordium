language: python
python:
  - "2.7"

env:
- LORDIUM_ENVIRONMENT=dev
# command to install dependencies

install:
  - pip install .
  - pip install -r requirements.txt
# command to run tests

script: python manage.py test